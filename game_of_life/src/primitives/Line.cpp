#include "Line.h"

Line::Line(glm::ivec2 start, glm::ivec2 end)
	: m_start(start), m_end(end) {}

glm::vec2 Line::interpolitePos(int width, int height)
{
	glm::vec2 vec;

	vec.x = m

	return glm::vec2();
}
